name: data_store_impl
description: "A package that provide local and online data storage implementations, and synchronization options."
version: 0.0.3
homepage: https://github.com/bienvenuelisis/data_store
repository: https://github.com/bienvenuelisis/data_store
issue_tracker: https://github.com/bienvenuelisis/data_store/issues

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  cloud_firestore: ^6.0.2
  firebase_core: ^4.1.1
  flutter:
    sdk: flutter
  localstore: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
